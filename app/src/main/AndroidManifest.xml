<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="org.techtown.newproject">

    <dist:module dist:instant="true" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".FinalActivity"
            android:label="@string/title_activity_final"
            android:theme="@style/AppTheme.NoActionBar"></activity>
        <activity
            android:name=".SelectActivity3"
            android:label="@string/title_activity_select3"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".SelectActivity2"
            android:label="@string/title_activity_select2"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".SelectActivity"
            android:label="@string/title_activity_select"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".MyRecipeDemo" />
        <activity android:name=".ListViewItem" />
        <activity android:name=".ListViewAdapter" />
        <activity android:name=".LoadingActivity" />
        <activity android:name=".foodlist" />
        <activity android:name=".RecipeActivity"/>
        <activity android:name=".CalendarActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>